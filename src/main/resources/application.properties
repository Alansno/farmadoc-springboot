#GENERAL
#El puerto deberia de ser el 80, ya que ese suele ser el que usa al subirlo a internet
server.port=8080
#
#DATABASE

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost/farmadoc?useSSL=false
spring.datasource.dbname=farmadoc
spring.datasource.username=root
spring.datasource.password=
spring.jpa.properties.hibernate.format_sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

logging.level.org.hibernate.SQL=DEBUG	
logging.level.org.hibernate.type=TRACE

spring.thymeleaf.mode=HTML5
spring.main.allow-circular-references=true

# JWT
security.jwt.secret=chinpigapatupumapadrepe1230
security.jwt.issuer=Main
# la sesion durara una semana
security.jwt.ttlMillis=604800000
